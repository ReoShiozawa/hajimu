表示("=== ジェネレータ共有状態テスト ===")

生成関数 数列(最大):
    変数 現在 = 1
    条件 現在 <= 最大 の間
        譲渡 現在
        現在 = 現在 + 1
    終わり
終わり

変数 g1 = 数列(3)
変数 g2 = g1

表示("g1次: " + 文字列化(次(g1)))
表示("g2次: " + 文字列化(次(g2)))
表示("g1残り: " + 文字列化(長さ(全値(g1))))
表示("g2完了: " + 文字列化(完了(g2)))
