# フィボナッチ数列サンプル
# 再帰関数と繰り返しのデモンストレーション

関数 フィボナッチ(n は 数値) は 数値:
    もし n <= 1 なら
        戻す n
    終わり
    戻す フィボナッチ(n - 1) + フィボナッチ(n - 2)
終わり

関数 メイン():
    表示("フィボナッチ数列")
    表示("================")
    
    i を 0 から 15 繰り返す
        表示("F(", i, ") =", フィボナッチ(i))
    終わり
終わり
