# 配列操作サンプル
# 配列の基本的な使い方

関数 メイン():
    表示("配列操作デモ")
    表示("============")
    
    # 配列の作成
    変数 数列 = [10, 20, 30, 40, 50]
    
    # 配列の要素を表示
    表示("配列の内容:")
    i を 0 から 長さ(数列) - 1 繰り返す
        表示("  数列[", i, "] =", 数列[i])
    終わり
    
    # 合計を計算
    変数 合計 = 0
    i を 0 から 長さ(数列) - 1 繰り返す
        合計 = 合計 + 数列[i]
    終わり
    
    # 結果を表示
    表示("合計:", 合計)
    表示("平均:", 合計 / 長さ(数列))
    
    # 配列に要素を追加
    追加(数列, 60)
    表示("追加後の長さ:", 長さ(数列))
終わり
